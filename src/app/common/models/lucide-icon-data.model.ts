import { ArrowDown } from 'lucide-angular';

//INFO: lucide-angular doesn't export the objekt LucideIconData, for not sure reason. This way the original type can still be used.
export type LucideIconData = typeof ArrowDown;
